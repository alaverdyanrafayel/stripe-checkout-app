import React, { Component } from 'react';

class NextButtonText extends Component {
    state = { text: 'SELECT PLAN' };

    render() {
        return this.state.text;
    }
}

export default NextButtonText;
